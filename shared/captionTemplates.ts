// shared/captionTemplates.ts

export const captionTemplates = [
  ({ overlay }: { overlay?: string }) =>
    `✨ ${overlay || 'Real life, real rhythm.'} ✨`,

  ({ overlay }: { overlay?: string }) =>
    `Not aesthetic. Not curated. Just soul-aligned. ${overlay || ''}`,

  ({ overlay }: { overlay?: string }) =>
    `POV: You stop forcing your life and start flowing. ${overlay || ''}`,

  ({ overlay }: { overlay?: string }) =>
    `You're not chaotic. You're cyclical. You're soul-led. ${overlay || ''}`,

  ({ overlay }: { overlay?: string }) =>
    `This is what healing looks like when you don't gatekeep it. ${overlay || ''}`,

  ({ overlay }: { overlay?: string }) =>
    `🌿 ${overlay || 'Welcome to Coyote Commons.'} Off-grid, on-rhythm.`,

  ({ overlay }: { overlay?: string }) =>
    `🧭 You’re not late. You’re right on time. Soul time. ${overlay || ''}`,

  ({ overlay }: { overlay?: string }) =>
    `📦 This rhythm system changed our whole house. (${overlay || 'Even the toddler loves it.'})`,

  ({ overlay }: { overlay?: string }) =>
    `Looping in my ancestors… they approved this post. ${overlay || ''}`,

  ({ overlay }: { overlay?: string }) =>
    `👁️ If you're reading this, you're remembering something. ${overlay || ''}`,
];